{
  "$merge": {
    "name": "${name}",
    "version": "${version}",
    "description": "${description}",
    "keywords": "${keywords}",
    "bugs": "https://github.com/${repo}/issues",
    "license": "MIT",
    "author": "${author}",
    "files": [
      "dist"
    ],
    "main": "./dist/index.js",
    "types": "./dist/index.d.ts",
    "repository": "${repo}",
    "scripts": {
      "$merge": {}
    },
    "dependencies": {
      "$merge": {},
      "$sort": true
    },
    "devDependencies": {
      "$merge": {
        "@types/node": "^18.7.3",
        "typescript": "^4.7.4"
      },
      "$sort": true
    }
  }
}
